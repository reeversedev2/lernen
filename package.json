{
  "name": "lernen",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel -r dev",
    "dev:docker": "docker-compose -f docker-compose.dev.yml up --build",
    "build": "pnpm -r build",
    "seed": "pnpm --filter @lernen/api seed",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.12",
    "typescript": "^5.4.5"
  }
}
